@inject IUserService service
@{
    ViewData["Title"] = "LoggedHome";
    var userNickname = this.service.GetUserNickname(this.User.Identity.Name);

    var model = this.service.GetHomeViewDetails();

}

<main>
    <div class="container d-flex justify-content-around">
        <div class="col-md-4" style="margin-top: 10em;">
            <div class="col-md-12 col-sm-12 border border-dark" style="min-height: 20em; min-width: 10em">
                <div class=" card-header text-center text-dark">Last Ten stories</div>
                <div class="card-body">
                    some text
                </div>
            </div>
        </div>
        <div class="col-md-4" style="margin-top: 10em;">
            <div class="col-md-12 col-sm-12 border border-dark" style="min-height: 20em; min-width: 10em">
                <div class=" card-header text-center text-dark"> User options</div>
                <div class="card-body">
                    <a asp-action="Profile" asp-controller="Users" asp-route-id="@userNickname" class="btn btn-info btn-block">My profile</a>
                    <a asp-action="UserStories" asp-controller="Stories" asp-route-id="@userNickname" class="btn btn-info btn-block">My Stories</a>
                    <a asp-action="UserMessages" asp-controller="Messages" asp-route-id="@userNickname" class="btn btn-info btn-block">My Messages</a>
                    <a asp-action="UserMessages" asp-controller="Messages" asp-route-id="@userNickname" class="btn btn-info btn-block">My Notifications</a>
                    <a asp-action="UserFriends" asp-controller="Users" asp-route-id="@userNickname" class="btn btn-info btn-block">Friends</a>
                    <a asp-action="AllStories" asp-controller="Stories" class="btn btn-info btn-block">All Stories</a>
                </div>
            </div>
        </div>
        <div class="col-md-4" style="margin-top: 10em;">
            <div class="col-md-12 col-sm-12 border border-dark" style="min-height: 20em; min-width: 10em">
                <div class=" card-header text-center text-dark">Announcements</div>
                <div class="card-body">
                    @if (model.Announcements.Count > 0)
                    {
                        for (int i = 0; i < @Model.Announcements.Count; i++)
                        {

                        }
                    }
                    else
                    {
                        <p>No current announcements</p>
                    }
                </div>
            </div>
        </div>
    </div>
</main>